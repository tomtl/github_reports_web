<!DOCTYPE html>
<html>
<head>
  <title>ReportsWeb</title>
  <%= javascript_include_tag "//code.jquery.com/jquery-2.1.3.min.js" %>
  <link href="//maxcdn.bootstrapcdn.com/bootstrap/3.3.4/css/bootstrap.min.css" rel="stylesheet">
  <script src="//maxcdn.bootstrapcdn.com/bootstrap/3.3.4/js/bootstrap.min.js"></script>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <div class="container">
    <nav class="navbar navbar-default navbar-static-top">
      <div class="navbar-header">
        <a class="navbar-brand" href="/">Web APIs</a>
      </div>
      <ul class="nav navbar-nav">
        <li><%= link_to "Gists", gists_path %></li>
      </ul>
      <% if user_signed_in? %>
        <p class="navbar-text">Signed in as <%= current_user.name %>. <%= link_to "Sign out", sessions_path, method: :delete %></p>
      <% else %>
        <p class="navbar-text"><%= link_to "Sign in with GitHub", '/auth/github' %></p>
      <% end %>
    </nav>
    <div class="row">
      <div class="col-sm-12 pull-right">
        <% flash.each do |name, msg| -%>
          <%= content_tag :div, msg, class: "alert alert-#{name}" %>
        <% end -%>
      </div>
    </div>
    <%= yield %>
  </div>
</body>
</html>
